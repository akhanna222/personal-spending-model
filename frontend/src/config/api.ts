// API Configuration
// Both frontend and backend are served from same domain in production
// Frontend proxies /api requests to backend via Nginx/Railway/Render
const API_BASE_URL = '/api';

export default API_BASE_URL;
